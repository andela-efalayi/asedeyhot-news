import React from 'react';
import AppActions from '../app/actions/appActions';
import App from '../app/actions/initialise';

describe('App ', () => {
  it('should be able to run tests', () => {
      expect(1 + 2).toEqual(3);
  });
});
